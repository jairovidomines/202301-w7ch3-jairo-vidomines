import "./TaskCard.css";

const TaskCard = (): JSX.Element => {
  return (
    <ul className="task">
      <span className="task__to-do">Clean the bathroom</span>
    </ul>
  );
};

export default TaskCard;
